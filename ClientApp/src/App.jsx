export default function App() {
  return (
    <main className="app-shell">
      <section className="hero-card">
        <p className="eyebrow">Quizletty</p>
        <h1>React quiz workspace is ready.</h1>
        <p>
          The next commits will turn this placeholder into a full quiz flow
          backed by the ASP.NET API.
        </p>
      </section>
    </main>
  );
}
